function nombre(){

		const name = prompt ('Ingreso el nombre Pedro');

		if (name == 'pedro')
				alert(`Excelente ingresaste el nombre ${name}`);

		else
				alert(`No ingresaste el nombre ${name}`);
}
